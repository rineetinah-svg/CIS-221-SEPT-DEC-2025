transactions=[    [1200,1350,1600,1450,1700,1800],    [1500,1600,1100,1300,1900,2000],    [900,1000,1200,1250,1400,1500],    [1800,1750,1600,1550,1650,1700]     ]print("Transaction Array:\n",transactions)total_per_branch=[sum(row) for row in transactions]print("Total per branch:",total_per_branch)highest_branch=total_per_branch.index(max(total_per_branch)) +1print("highest_branch",highest_branch)all_values=[value for row in transactions for value in row]average_monthly=sum(all_values)/len(all_values)print("Average monthly transactions:",average_monthly)flat=[value for row in transactions for value in row]reshaped=[flat[i:i+8] for i in range(0,len(flat),8)]print("\nReshaped Array (3*8):")for row in reshaped:    print(row)